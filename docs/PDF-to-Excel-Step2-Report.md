# PDF→Excel Step 2 検証レポート: 罫線抽出プロトタイプ

> ハンドオフ資料 (`28yu/Revit-Add-ins:Docs/HANDOFF-PDFtoExcel-Tool.md`) の **Step 2 (A-2 検証)** の実証結果。

## TL;DR

| 項目 | 結果 |
|---|---|
| **罫線抽出** | ✅ PDF.js `operatorList` から線分を確実に取得できる |
| **座標統一** | ✅ ページ回転を含む座標変換が機能する |
| **セル化** | ✅ 行列クラスタリング → セルグリッド → テキスト割当が動作 |
| **判定** | ✅ **本実装フェーズへ進行可能** |
| **新たに判明した方針修正** | ⚠️ PDF2 p.1 のレイアウトは **「均一な大グリッド」ではなく「カード集合」**。次フェーズで「カード境界検出」ステップが必要 |

## 検証対象

- 入力: PDF2 p.1（基礎大梁リスト、A3 横置き 1191×842pt、`rotate=90`）
- スコープ: ハンドオフで指定された通り、本ページ 1 ページのみ

## 実装

プロトタイプ実体は `pdf-to-excel/prototype/` 配下。

1. **`extract.mjs`** — `operatorList` を走査し罫線を線分配列として収集
2. **`render.mjs`** — 検出した罫線をページレンダリング上にオーバーレイ表示
3. **`grid.mjs`** — 行列クラスタ → セル割当 → CSV ダンプ

## 数値結果

| 指標 | 値 |
|---|---|
| operatorList 総 op 数 | 28,831 |
| 抽出した横線 | 305 |
| 抽出した縦線 | 470 |
| クラスタリング後の行候補（全ページ） | 70 |
| クラスタリング後の列候補（全ページ） | 171 |
| band 1 (FG1〜FG6 帯) のセル | 14 行 × 86 列 |
| band 1 で実際にテキストが入ったセル | 約 100 |

### band 1 抽出サンプル

| 行 | y | 列パターン | 内容 |
|---|---|---|---|
| r1 | 223 | D25, D25, D25, D25, D25, D25 | 主筋径 |
| r2 | 233 | 6+2, 6+3, 8, 6+1, 6, 6+2, ... | 主筋上 1段+2段 |
| r3 | 243 | 6, 6+3, 8+3, 6, 6+3, 6+2, ... | 主筋下 |
| r4 | 253 | -, 3,800, -, -, 3,800, -, -, -, -, 4,400, ... | 端あき寸法 |
| r5 | 263 | 2,800, 2,900, 2,600, 3,000, 2,600, ... | 端あき寸法 |

→ **ハンドオフ資料の「PDF2 p.1 は完璧に抽出できる」検証結果と整合**。罫線駆動の表セル化で構造化データとして取得可能。

## 実装上の重要な学び

### 1. 罫線が「fill された薄い rectangle」で描画される

構造図 PDF では罫線が `stroke` ではなく、**幅 0.5pt 程度の薄い filled rectangle** として描画されることが多い。`stroke` だけを拾う実装は取りこぼす。

```
constructPath [[19], [805.747, 1008.059, 0.459, 2.660], ...]
fill
```

→ `OPS.fill` / `OPS.eoFill` 後の最後の `constructPath` も処理対象に含める。

### 2. PathOps の内部コードは OPS テーブルとは別

`constructPath` の `args[0]` に格納される内部コード（PathOps）は、`OPS` 名前空間とは別系統:

| コード | 意味 |
|---|---|
| 13 | moveTo |
| 14 | lineTo |
| 15 | curveTo（6 引数） |
| 16 | curveTo2（4 引数） |
| 17 | curveTo3（4 引数） |
| 18 | closePath |
| 19 | rectangle（4 引数: x, y, w, h） |

これは `pdfjs-dist` の TypeScript 定義には型としては公開されていないので、ソース (`src/shared/util.ts` の `PathOps`) 参照。

### 3. ページ回転と座標変換

PDF2 p.1 は `page.rotate=90` で、MediaBox は 842×1191（縦）だが、表示は 1191×842（横）。

- `operatorList` の座標は **MediaBox 座標**
- 表示・ユーザー座標に変換するには `page.getViewport().transform` を適用する必要がある
- 加えて、各 op の CTM スタック (`save`/`restore`/`transform`) を追跡してからの最終変換

### 4. PDF2 p.1 のレイアウトは「カード集合」

最も重要な学び。当初の想定（Excel風の uniform table）と異なり、実際は:

```
[行ラベル列 | FG1カード | FG2カード | FG3カード | ... ]
            ├ 断面図   ├ 断面図   ├ 断面図
            ├ 主筋径   ├ 主筋径   ├ 主筋径
            ├ a|中央|b ├ a|中央|b ├ a|中央|b|c|d (連スパン)
            └ ...     └ ...     └ ...
```

- 各 FG は**独立した小カード**で、カード内に「位置 × 配筋プロパティ」のサブテーブルを持つ
- 連スパン梁（例: FG17,18,(19)）はカード幅が大きく、内部列数も変わる
- カード横断で共通する罫線（行ラベル列の境界、最上段の断面図ベースライン）と、カード固有の罫線（位置区切り）が混在する

→ 単純な「全 H 線をクラスタ → 全 V 線をクラスタ → グリッドにする」だけでは破綻する。次フェーズで **「カード境界検出」ステップ** が必要。

## 次フェーズに向けた方針修正

### 改訂アーキテクチャ案

```
1. 罫線抽出（本検証で完了）
   ↓
2. カード境界検出  ← 新規ステップ
   - 外枠 rectangle を見つける（PDF が rect として明示しているか、ヒューリスティックで矩形領域を判定）
   - または「行ラベル列」を特定し、横方向に均等間隔で繰り返される領域パターンを認識
   ↓
3. カードごとに内部グリッド構築
   - カード内に閉じた行・列クラスタリング
   - 行ラベル列のテキスト（主筋径/主筋上/主筋下/端あき/...）を取得
   - 位置列のラベル取得（a/中央/b/...）
   ↓
4. スキーマ列にマップ
   - 行ラベル + 位置ラベル → スキーマ v1 の `x_主筋径` / `x_上_1段` 等
   - パース後と原文の両方を出力
   ↓
5. プレビュー UI でユーザー確認・修正
   ↓
6. SheetJS で .xlsx 生成
```

### 次に取り組むタスク（優先順）

1. **カード境界検出ロジック**（最重要）
   - PDF2 p.1 の各 FG カードの外枠 bounding box を自動検出する
   - 出力: 各カードの (xMin, yMin, xMax, yMax) リスト
2. **カード内サブグリッド構築**
   - 行ラベル列（最左）のテキスト「主筋径」「主筋上」「主筋下」「あばら筋」「端あき」を取得
   - 位置列のラベル（a/中央/b/...）を取得
   - 行 × 位置のセルを構築
3. **配筋パーサー実装**（スキーマ v1 仕様）
   - `D25` → `主筋径=D25`
   - `6+2` → `1段=6, 2段=2`
   - `D13[2]@150` → `あばら径=D13, 脚数=2, ピッチ=150`
4. **S 梁ページ (PDF2 p.2) の検証**
   - レイアウトが RC 梁と異なる（カード型ではなく行型かもしれない）ため別途検証必要
5. **OCR フォールバック検証**（PDF1 全壊ページ用、Step 3）

## 視覚証拠

- `pdf-to-excel/prototype/sample-output/page1-overlay.png` — PDF2 p.1 全面のオーバーレイ（赤=横線、青=縦線）
- `pdf-to-excel/prototype/sample-output/zoom-fg1.png` — FG1 周辺のズーム。検出した罫線が visual な表のセル境界とほぼ完全一致

## 改訂履歴

| バージョン | 日付 | 内容 |
|---|---|---|
| v1 | 2026-05-20 | Step 2 検証完了レポート初版 |
